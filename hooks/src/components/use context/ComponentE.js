import React, { useContext } from 'react';
import { userContext } from '../../App';

const ComponentE = () => {
//   const user = useContext(userContext);

  return (
    // <div>{user ? <p>Hello, {user}!</p> : <p>No user data available.</p>}</div>
    hello
  );
};

export default ComponentE;
